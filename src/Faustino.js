import React, { Component } from 'react';
import {Text, View} from 'react-native';

export default class Faustino extends Component {
    render(){
        return(
            <Text>Opa</Text>
        );
    }
}